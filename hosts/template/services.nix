{
  # Add or remove references to services to modify functionality.
  # These differ from modules logically by the way a user interacts with them
  # rather than the options implying a program to be used or alike.
  imports =
    [ ../../services/clamav ../../services/openssh ../../services/pipewire ];
}
